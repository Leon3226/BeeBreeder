import { TransposerWithInventories } from './transposer-with-inventories';

describe('TransposerWithInventories', () => {
  it('should create an instance', () => {
    expect(new TransposerWithInventories()).toBeTruthy();
  });
});
